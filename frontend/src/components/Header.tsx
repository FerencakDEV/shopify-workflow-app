import ContentHeader from './ContentHeader';
import NavBar from './NavBar';

const Header = () => {
  return (
    <header>
      <ContentHeader />
      <NavBar />
    </header>
  );
};

export default Header;
