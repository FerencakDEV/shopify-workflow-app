import ContentHeader from './ContentHeader';
import NavBar from '../components/Navbar';

const Header = () => {
  return (
    <header>
      <ContentHeader />
      <NavBar />
    </header>
  );
};

export default Header;
