import ContentHeader from './ContentHeader';
import NavBar from './NavigationBar';

const Header = () => {
  return (
    <header>
      <ContentHeader />
      <NavBar />
    </header>
  );
};

export default Header;
